@* if you want to return the new category object to the view (in the create action method) but it is not required. eg: return View(new catogory)
If nothing is return in that method then it will return the view with the default values ( mostly empty) of the model properties
by writing the below code.(@model Category) *@
 @model Category
 <form method="post">
     <div class="border p-3 mt-4">
         <div class="row pb-2">
            <h2 class="text-primary">Create Category</h2>
            <hr/>
        </div>
        @* <div asp-validation-summary="ModelOnly"></div> *@
        <div class="mb-3 row p-1">
            @* <label class="p-0">Category Name</label> *@
            <label asp-for="Name" class="p-0"></label>
            <input asp-for="Name" class="form-control"/>
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="mb-3 row p-1">
            @* <label class="p-0">Display Order </label> *@
            <label asp-for="DisplayOrder" class="p-0"></label>
            <input asp-for="DisplayOrder" class="form-control"/>
            <span asp-validation-for="DisplayOrder" class="text-danger"></span>
        </div>
        <div class="row">
            <div class="col-6 col-md-3">
                <button type="submit" class="btn btn-primary form-control">Create</button>
            </div>
            <div class="col-6 col-md-3">
                <a asp-controller="=Category" asp-action="Index" class="btn btn-outline-secondary border form-control">
                    Back to list
                </a>
            </div>

        </div>
     </div>
 </form>

 @section scripts{
     @{
        <partial name="_ValidationScriptsPartial" />
     }
 }
